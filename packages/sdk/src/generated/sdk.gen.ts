// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type Options as Options2, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { BatchCreateBlocksData, BatchCreateBlocksErrors, BatchCreateBlocksResponses, CreateBlockCommentData, CreateBlockCommentErrors, CreateBlockCommentResponses, CreateBlockData, CreateBlockErrors, CreateBlockResponses, CreateChannelData, CreateChannelErrors, CreateChannelResponses, CreateConnectionData, CreateConnectionErrors, CreateConnectionResponses, CreateGroupData, CreateGroupErrors, CreateGroupInvitationData, CreateGroupInvitationErrors, CreateGroupInvitationResponses, CreateGroupInviteData, CreateGroupInviteErrors, CreateGroupInviteResponses, CreateGroupResponses, CreateOAuthTokenData, CreateOAuthTokenErrors, CreateOAuthTokenResponses, DeleteChannelData, DeleteChannelErrors, DeleteChannelResponses, DeleteCommentData, DeleteCommentErrors, DeleteCommentResponses, DeleteConnectionData, DeleteConnectionErrors, DeleteConnectionResponses, DeleteGroupData, DeleteGroupErrors, DeleteGroupInviteData, DeleteGroupInviteErrors, DeleteGroupInviteResponses, DeleteGroupResponses, GetBatchStatusData, GetBatchStatusErrors, GetBatchStatusResponses, GetBlockCommentsData, GetBlockCommentsErrors, GetBlockCommentsResponses, GetBlockConnectionsData, GetBlockConnectionsErrors, GetBlockConnectionsResponses, GetBlockData, GetBlockErrors, GetBlockResponses, GetChannelConnectionsData, GetChannelConnectionsErrors, GetChannelConnectionsResponses, GetChannelContentsData, GetChannelContentsErrors, GetChannelContentsResponses, GetChannelData, GetChannelErrors, GetChannelFollowersData, GetChannelFollowersErrors, GetChannelFollowersResponses, GetChannelResponses, GetConnectionData, GetConnectionErrors, GetConnectionResponses, GetCurrentUserData, GetCurrentUserErrors, GetCurrentUserResponses, GetGroupContentsData, GetGroupContentsErrors, GetGroupContentsResponses, GetGroupData, GetGroupErrors, GetGroupFollowersData, GetGroupFollowersErrors, GetGroupFollowersResponses, GetGroupInvitationsData, GetGroupInvitationsErrors, GetGroupInvitationsResponses, GetGroupInviteData, GetGroupInviteErrors, GetGroupInviteResponses, GetGroupMembersData, GetGroupMembersErrors, GetGroupMembersResponses, GetGroupResponses, GetMyFeedData, GetMyFeedErrors, GetMyFeedResponses, GetMyNotificationsData, GetMyNotificationsErrors, GetMyNotificationsResponses, GetOpenapiSpecData, GetOpenapiSpecErrors, GetOpenapiSpecJsonData, GetOpenapiSpecJsonErrors, GetOpenapiSpecJsonResponses, GetOpenapiSpecResponses, GetPingData, GetPingErrors, GetPingResponses, GetUserContentsData, GetUserContentsErrors, GetUserContentsResponses, GetUserData, GetUserErrors, GetUserFollowersData, GetUserFollowersErrors, GetUserFollowersResponses, GetUserFollowingData, GetUserFollowingErrors, GetUserFollowingResponses, GetUserGroupsData, GetUserGroupsErrors, GetUserGroupsResponses, GetUserResponses, JoinGroupData, JoinGroupErrors, JoinGroupResponses, LeaveGroupData, LeaveGroupErrors, LeaveGroupResponses, MarkAllMyNotificationsReadData, MarkAllMyNotificationsReadErrors, MarkAllMyNotificationsReadResponses, MarkMyNotificationReadData, MarkMyNotificationReadErrors, MarkMyNotificationReadResponses, MoveConnectionData, MoveConnectionErrors, MoveConnectionResponses, PresignUploadData, PresignUploadErrors, PresignUploadResponses, RemoveGroupMemberData, RemoveGroupMemberErrors, RemoveGroupMemberResponses, RevokeGroupInvitationData, RevokeGroupInvitationErrors, RevokeGroupInvitationResponses, SearchData, SearchErrors, SearchResponses, UpdateBlockData, UpdateBlockErrors, UpdateBlockResponses, UpdateChannelData, UpdateChannelErrors, UpdateChannelResponses, UpdateConnectionData, UpdateConnectionErrors, UpdateConnectionResponses, UpdateGroupData, UpdateGroupErrors, UpdateGroupResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Obtain access token
 *
 * Exchange credentials for an access token. This is the OAuth 2.0 token endpoint.
 *
 * **Supported Grant Types:**
 *
 * - `authorization_code`: Exchange an authorization code for an access token
 * - `authorization_code` + PKCE: For public clients without a client secret
 * - `client_credentials`: Authenticate as your application (server-to-server)
 *
 * **PKCE Support:** For public clients (mobile apps, SPAs), use PKCE:
 * 1. Generate a random `code_verifier` (43-128 chars, alphanumeric + `-._~`)
 * 2. Create `code_challenge` = Base64URL(SHA256(code_verifier))
 * 3. In the authorization request to `https://www.are.na/oauth/authorize`, include:
 * - `code_challenge`: The generated challenge
 * - `code_challenge_method`: `S256`
 * 4. When exchanging the code at this endpoint, include `code_verifier`
 *
 * See [RFC 7636](https://tools.ietf.org/html/rfc7636) for details.
 *
 * Access tokens do not expire and can be used indefinitely. Register your application
 * at [are.na/oauth/applications](https://www.are.na/oauth/applications) to obtain client credentials.
 *
 */
export const createOAuthToken = <ThrowOnError extends boolean = false>(options: Options<CreateOAuthTokenData, ThrowOnError>) => (options.client ?? client).post<CreateOAuthTokenResponses, CreateOAuthTokenErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/v3/oauth/token',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get OpenAPI specification
 *
 * Returns the OpenAPI 3.0 specification for this API in YAML format. This endpoint provides the complete API contract for programmatic access and documentation generation.
 */
export const getOpenapiSpec = <ThrowOnError extends boolean = false>(options?: Options<GetOpenapiSpecData, ThrowOnError>) => (options?.client ?? client).get<GetOpenapiSpecResponses, GetOpenapiSpecErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/openapi',
    ...options
});

/**
 * Get OpenAPI specification (JSON)
 *
 * Returns the OpenAPI 3.0 specification for this API in JSON format. This endpoint provides the complete API contract in JSON for tools that prefer JSON over YAML.
 */
export const getOpenapiSpecJson = <ThrowOnError extends boolean = false>(options?: Options<GetOpenapiSpecJsonData, ThrowOnError>) => (options?.client ?? client).get<GetOpenapiSpecJsonResponses, GetOpenapiSpecJsonErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/openapi.json',
    ...options
});

/**
 * Ping endpoint
 *
 * Public utility endpoint for API health checks and connection testing.
 */
export const getPing = <ThrowOnError extends boolean = false>(options?: Options<GetPingData, ThrowOnError>) => (options?.client ?? client).get<GetPingResponses, GetPingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/ping',
    ...options
});

/**
 * Get a presigned upload URL
 *
 * Returns presigned S3 PUT URLs for direct file upload. Use this to upload
 * files (images, attachments) without sending them through the API server.
 * Supports up to 50 files per request.
 *
 * **Upload flow:**
 * 1. Call this endpoint with an array of files (filename + content type)
 * 2. PUT each file's bytes to its returned `upload_url` with the matching `Content-Type` header
 * 3. Create blocks via `POST /v3/blocks` with `value` set to the S3 URL:
 * `https://s3.amazonaws.com/arena_images-temp/<key>`
 *
 * Presigned URLs expire after 1 hour.
 *
 * **Authentication required.**
 *
 */
export const presignUpload = <ThrowOnError extends boolean = false>(options: Options<PresignUploadData, ThrowOnError>) => (options.client ?? client).post<PresignUploadResponses, PresignUploadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/uploads/presign',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a block
 *
 * Creates a new block and connects it to one or more channels.
 *
 * The `value` field accepts either a URL or text content:
 * - If `value` is a valid URL, the block type is inferred (Image, Link, Embed, etc.)
 * - If `value` is plain text, a Text block is created
 *
 * You can connect the block to multiple channels at once (up to 20).
 *
 * Specify target channels using either `channels` (preferred) or `channel_ids` (legacy), but not both.
 * The `channels` form allows per-channel position and connection metadata.
 *
 * **Authentication required.**
 *
 */
export const createBlock = <ThrowOnError extends boolean = false>(options: Options<CreateBlockData, ThrowOnError>) => (options.client ?? client).post<CreateBlockResponses, CreateBlockErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Batch create blocks
 *
 * Queues multiple blocks for asynchronous creation and connects them to one
 * or more channels. Returns immediately with a `batch_id` that can be used
 * to poll for status via `GET /v3/blocks/batch/{batch_id}`.
 *
 * Designed for import use cases such as migrating from other services
 * or re-importing Are.na exports.
 *
 * Each block in the `blocks` array follows the same format as single block
 * creation. Blocks are processed sequentially in the background and partial
 * success is supported — if some blocks fail, the successful ones are still
 * created.
 *
 * **Limits:**
 * - Maximum 50 blocks per request
 * - Maximum 20 channels per request
 * - All channels must be **private**
 *
 * **Batch results are available for 24 hours after submission.**
 *
 * **⚠️ Premium Only**: This endpoint requires a Premium subscription.
 *
 */
export const batchCreateBlocks = <ThrowOnError extends boolean = false>(options: Options<BatchCreateBlocksData, ThrowOnError>) => (options.client ?? client).post<BatchCreateBlocksResponses, BatchCreateBlocksErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/batch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get batch status
 *
 * Returns the current status of a batch block creation job.
 * Poll this endpoint to track progress and retrieve results.
 *
 * Batch results are available for 24 hours after submission.
 *
 * **⚠️ Premium Only**: This endpoint requires a Premium subscription.
 *
 */
export const getBatchStatus = <ThrowOnError extends boolean = false>(options: Options<GetBatchStatusData, ThrowOnError>) => (options.client ?? client).get<GetBatchStatusResponses, GetBatchStatusErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/batch/{batch_id}',
    ...options
});

/**
 * Get a block
 *
 * Returns detailed information about a specific block by its ID. Respects visibility rules and user permissions.
 */
export const getBlock = <ThrowOnError extends boolean = false>(options: Options<GetBlockData, ThrowOnError>) => (options.client ?? client).get<GetBlockResponses, GetBlockErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/{id}',
    ...options
});

/**
 * Update a block
 *
 * Updates a block's metadata. Only the block owner can update it.
 *
 * **Authentication required.**
 *
 */
export const updateBlock = <ThrowOnError extends boolean = false>(options: Options<UpdateBlockData, ThrowOnError>) => (options.client ?? client).put<UpdateBlockResponses, UpdateBlockErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get block connections
 *
 * Returns paginated list of channels where this block appears.
 * This shows all channels that contain this block, respecting visibility rules and user permissions.
 *
 */
export const getBlockConnections = <ThrowOnError extends boolean = false>(options: Options<GetBlockConnectionsData, ThrowOnError>) => (options.client ?? client).get<GetBlockConnectionsResponses, GetBlockConnectionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/{id}/connections',
    ...options
});

/**
 * Get block comments
 *
 * Returns paginated list of comments on this block.
 * Comments are ordered by creation date (ascending by default, oldest first).
 *
 */
export const getBlockComments = <ThrowOnError extends boolean = false>(options: Options<GetBlockCommentsData, ThrowOnError>) => (options.client ?? client).get<GetBlockCommentsResponses, GetBlockCommentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/{id}/comments',
    ...options
});

/**
 * Create a comment
 *
 * Creates a new comment on a block.
 *
 * **Authentication required.**
 *
 * You can mention users in the comment body using `@username` syntax.
 *
 */
export const createBlockComment = <ThrowOnError extends boolean = false>(options: Options<CreateBlockCommentData, ThrowOnError>) => (options.client ?? client).post<CreateBlockCommentResponses, CreateBlockCommentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/blocks/{id}/comments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a comment
 *
 * Deletes a comment. Only the comment author can delete their comments.
 *
 * **Authentication required.**
 *
 */
export const deleteComment = <ThrowOnError extends boolean = false>(options: Options<DeleteCommentData, ThrowOnError>) => (options.client ?? client).delete<DeleteCommentResponses, DeleteCommentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/comments/{id}',
    ...options
});

/**
 * Create a channel
 *
 * Creates a new channel owned by the authenticated user or a group they belong to.
 *
 * **Authentication required.**
 *
 */
export const createChannel = <ThrowOnError extends boolean = false>(options: Options<CreateChannelData, ThrowOnError>) => (options.client ?? client).post<CreateChannelResponses, CreateChannelErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/channels',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a channel
 *
 * Deletes a channel. This can not be undone.
 *
 * **Authentication required.**
 *
 */
export const deleteChannel = <ThrowOnError extends boolean = false>(options: Options<DeleteChannelData, ThrowOnError>) => (options.client ?? client).delete<DeleteChannelResponses, DeleteChannelErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/channels/{id}',
    ...options
});

/**
 * Get a channel
 *
 * Returns detailed information about a specific channel by its ID or slug. Respects visibility rules and user permissions.
 */
export const getChannel = <ThrowOnError extends boolean = false>(options: Options<GetChannelData, ThrowOnError>) => (options.client ?? client).get<GetChannelResponses, GetChannelErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/channels/{id}',
    ...options
});

/**
 * Update a channel
 *
 * Updates an existing channel. Only provided fields are updated.
 * Updating the owner requires permission to assign authorship to the target user or group.
 *
 * **Authentication required.**
 *
 */
export const updateChannel = <ThrowOnError extends boolean = false>(options: Options<UpdateChannelData, ThrowOnError>) => (options.client ?? client).put<UpdateChannelResponses, UpdateChannelErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/channels/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a connection
 *
 * Connects a block or channel to one or more channels.
 * Returns the created connection(s).
 *
 * Specify target channels using either `channels` (preferred) or `channel_ids` (legacy), but not both.
 * The `channels` form allows per-channel position and connection metadata.
 *
 * **Authentication required.**
 *
 */
export const createConnection = <ThrowOnError extends boolean = false>(options: Options<CreateConnectionData, ThrowOnError>) => (options.client ?? client).post<CreateConnectionResponses, CreateConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/connections',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a connection
 *
 * Removes a block or channel from a channel by deleting the connection.
 * The block/channel itself is not deleted - it may still exist in other channels.
 *
 * **Authentication required.**
 *
 */
export const deleteConnection = <ThrowOnError extends boolean = false>(options: Options<DeleteConnectionData, ThrowOnError>) => (options.client ?? client).delete<DeleteConnectionResponses, DeleteConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/connections/{id}',
    ...options
});

/**
 * Get connection details
 *
 * Returns detailed information about a connection,
 * including abilities (whether the current user can remove the connection).
 *
 * The connection ID is included in the `connection` object when blocks/channels
 * are returned as part of channel contents.
 *
 */
export const getConnection = <ThrowOnError extends boolean = false>(options: Options<GetConnectionData, ThrowOnError>) => (options.client ?? client).get<GetConnectionResponses, GetConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/connections/{id}',
    ...options
});

/**
 * Update a connection
 *
 * Updates a connection's metadata. Uses merge semantics: new keys are added,
 * existing keys are updated, keys set to null are removed.
 *
 * **Authentication required.**
 *
 */
export const updateConnection = <ThrowOnError extends boolean = false>(options: Options<UpdateConnectionData, ThrowOnError>) => (options.client ?? client).put<UpdateConnectionResponses, UpdateConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/connections/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Move a connection
 *
 * Moves a connection to a new position within its channel.
 * Requires sort permission on the channel.
 *
 * **Authentication required.**
 *
 */
export const moveConnection = <ThrowOnError extends boolean = false>(options: Options<MoveConnectionData, ThrowOnError>) => (options.client ?? client).post<MoveConnectionResponses, MoveConnectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/connections/{id}/move',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get channel contents
 *
 * Returns paginated contents (blocks and channels) from a channel.
 * Respects visibility rules and user permissions.
 *
 */
export const getChannelContents = <ThrowOnError extends boolean = false>(options: Options<GetChannelContentsData, ThrowOnError>) => (options.client ?? client).get<GetChannelContentsResponses, GetChannelContentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/channels/{id}/contents',
    ...options
});

/**
 * Get channel connections
 *
 * Returns paginated list of channels where this channel appears.
 * This shows all channels that contain this channel, respecting visibility rules and user permissions.
 *
 */
export const getChannelConnections = <ThrowOnError extends boolean = false>(options: Options<GetChannelConnectionsData, ThrowOnError>) => (options.client ?? client).get<GetChannelConnectionsResponses, GetChannelConnectionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/channels/{id}/connections',
    ...options
});

/**
 * Get channel followers
 *
 * Returns paginated list of users who follow this channel.
 * All followers are users.
 *
 */
export const getChannelFollowers = <ThrowOnError extends boolean = false>(options: Options<GetChannelFollowersData, ThrowOnError>) => (options.client ?? client).get<GetChannelFollowersResponses, GetChannelFollowersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/channels/{id}/followers',
    ...options
});

/**
 * Get current user
 *
 * Returns the currently authenticated user's profile, including the
 * token holder's `email` address. The `email` field is only exposed
 * to the user themselves and is not present on `/v3/users/{id}` or on
 * embedded `User` references in other resources.
 *
 */
export const getCurrentUser = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentUserResponses, GetCurrentUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/me',
    ...options
});

/**
 * Get current user's feed
 *
 * Returns the authenticated user's primary activity feed.
 * This feed is personalized and requires authentication.
 *
 */
export const getMyFeed = <ThrowOnError extends boolean = false>(options?: Options<GetMyFeedData, ThrowOnError>) => (options?.client ?? client).get<GetMyFeedResponses, GetMyFeedErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/me/feed',
    ...options
});

/**
 * Get current user's notifications
 *
 * Returns the authenticated user's notifications feed.
 * Each notification includes read state for the current user.
 *
 */
export const getMyNotifications = <ThrowOnError extends boolean = false>(options?: Options<GetMyNotificationsData, ThrowOnError>) => (options?.client ?? client).get<GetMyNotificationsResponses, GetMyNotificationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/me/notifications',
    ...options
});

/**
 * Mark all current user's notifications as read
 *
 * Clears the authenticated user's unread notification state and returns
 * the new unread count.
 *
 */
export const markAllMyNotificationsRead = <ThrowOnError extends boolean = false>(options?: Options<MarkAllMyNotificationsReadData, ThrowOnError>) => (options?.client ?? client).post<MarkAllMyNotificationsReadResponses, MarkAllMyNotificationsReadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/me/notifications/read',
    ...options
});

/**
 * Mark a notification as read
 *
 * Marks one notification activity as read for the authenticated user
 * and returns the updated notification plus the new unread count.
 * The `id` is the activity id returned by `/v3/me/notifications`; the
 * internal notification bulletin id remains opaque. Returns 404 if the
 * activity is not part of the authenticated user's notifications feed.
 *
 */
export const markMyNotificationRead = <ThrowOnError extends boolean = false>(options: Options<MarkMyNotificationReadData, ThrowOnError>) => (options.client ?? client).post<MarkMyNotificationReadResponses, MarkMyNotificationReadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/me/notifications/{id}/read',
    ...options
});

/**
 * Get a user
 *
 * Returns detailed information about a specific user by their slug. Includes user profile, bio, and counts.
 */
export const getUser = <ThrowOnError extends boolean = false>(options: Options<GetUserData, ThrowOnError>) => (options.client ?? client).get<GetUserResponses, GetUserErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/users/{id}',
    ...options
});

/**
 * Get user contents
 *
 * Returns paginated contents (blocks and channels) created by a user.
 * Uses the search API to find all content added by the specified user.
 * Respects visibility rules and user permissions.
 */
export const getUserContents = <ThrowOnError extends boolean = false>(options: Options<GetUserContentsData, ThrowOnError>) => (options.client ?? client).get<GetUserContentsResponses, GetUserContentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/users/{id}/contents',
    ...options
});

/**
 * Get user followers
 *
 * Returns paginated list of users who follow this user.
 * All followers are users.
 *
 */
export const getUserFollowers = <ThrowOnError extends boolean = false>(options: Options<GetUserFollowersData, ThrowOnError>) => (options.client ?? client).get<GetUserFollowersResponses, GetUserFollowersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/users/{id}/followers',
    ...options
});

/**
 * Get user following
 *
 * Returns paginated list of users, channels, and groups that this user follows.
 * Can be filtered by type to return only specific followable types.
 *
 */
export const getUserFollowing = <ThrowOnError extends boolean = false>(options: Options<GetUserFollowingData, ThrowOnError>) => (options.client ?? client).get<GetUserFollowingResponses, GetUserFollowingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/users/{id}/following',
    ...options
});

/**
 * Get user groups
 *
 * Returns paginated list of groups the user belongs to (as owner or member).
 * When authenticated as the target user, includes private groups.
 * Otherwise only public groups are returned.
 *
 */
export const getUserGroups = <ThrowOnError extends boolean = false>(options: Options<GetUserGroupsData, ThrowOnError>) => (options.client ?? client).get<GetUserGroupsResponses, GetUserGroupsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/users/{id}/groups',
    ...options
});

/**
 * Create a group
 *
 * Creates a new group owned by the authenticated user.
 *
 * **Authentication required.**
 *
 */
export const createGroup = <ThrowOnError extends boolean = false>(options: Options<CreateGroupData, ThrowOnError>) => (options.client ?? client).post<CreateGroupResponses, CreateGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a group
 *
 * Deletes a group. This can not be undone.
 *
 * **Authentication required.**
 *
 */
export const deleteGroup = <ThrowOnError extends boolean = false>(options: Options<DeleteGroupData, ThrowOnError>) => (options.client ?? client).delete<DeleteGroupResponses, DeleteGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}',
    ...options
});

/**
 * Get a group
 *
 * Returns detailed information about a specific group by its slug. Includes group profile, description, owner, counts, and permissions.
 */
export const getGroup = <ThrowOnError extends boolean = false>(options: Options<GetGroupData, ThrowOnError>) => (options.client ?? client).get<GetGroupResponses, GetGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}',
    ...options
});

/**
 * Update a group
 *
 * Updates an existing group. Only provided fields are updated.
 *
 * **Authentication required.**
 *
 */
export const updateGroup = <ThrowOnError extends boolean = false>(options: Options<UpdateGroupData, ThrowOnError>) => (options.client ?? client).put<UpdateGroupResponses, UpdateGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get group members
 *
 * Returns a paginated list of group members. The group owner is always
 * returned as the first entry on page 1 with `role: "owner"`, followed by
 * collaborators in newest-first order with `role: "member"`. The total
 * count includes the owner.
 *
 * Group managers see all collaborators; other readers only see
 * collaborators with confirmed accounts. The owner is always shown.
 *
 */
export const getGroupMembers = <ThrowOnError extends boolean = false>(options: Options<GetGroupMembersData, ThrowOnError>) => (options.client ?? client).get<GetGroupMembersResponses, GetGroupMembersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/members',
    ...options
});

/**
 * Join a group
 *
 * Adds the authenticated user to the group as a member. Requires a valid
 * group invite token from `GET/POST /v3/groups/{id}/invite`; the token is
 * the authorization to join, so it may grant access to a private group.
 * Idempotent: if the authenticated user is already the owner or a member,
 * the endpoint returns `200` with that user's group-member entry.
 *
 * **Authentication required.**
 *
 */
export const joinGroup = <ThrowOnError extends boolean = false>(options: Options<JoinGroupData, ThrowOnError>) => (options.client ?? client).post<JoinGroupResponses, JoinGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/members',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Leave a group
 *
 * Removes the authenticated user from the group. Idempotent: returns
 * `204` whether or not the user was a member.
 *
 * **Authentication required.**
 *
 */
export const leaveGroup = <ThrowOnError extends boolean = false>(options: Options<LeaveGroupData, ThrowOnError>) => (options.client ?? client).delete<LeaveGroupResponses, LeaveGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/members/me',
    ...options
});

/**
 * Remove a group member
 *
 * Removes a user from a group.
 *
 * **Authentication required.**
 *
 */
export const removeGroupMember = <ThrowOnError extends boolean = false>(options: Options<RemoveGroupMemberData, ThrowOnError>) => (options.client ?? client).delete<RemoveGroupMemberResponses, RemoveGroupMemberErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/members/{user_id}',
    ...options
});

/**
 * List pending group invitations
 *
 * Returns a paginated list of open (pending) membership invitations for
 * the group, newest first. Only users who can manage invitations on the
 * group — typically the owner and existing members — may list them.
 *
 * Use `POST /v3/groups/{id}/invitations` to create an invitation; that
 * endpoint chooses between direct add and invitation automatically based
 * on the follow relationship.
 *
 * ### Invitation acceptance flow
 *
 * Invitees receive an email with a tokenized URL that opens the Are.na web
 * UI. Accept and decline actions are handled there via GraphQL
 * (`acceptMembershipInvitation` and `declineMembershipInvitation`), not
 * through this REST API. There is no REST endpoint for invitees to list
 * their own pending invitations today; invitation discovery is email-link
 * based.
 *
 * **Authentication required.**
 *
 */
export const getGroupInvitations = <ThrowOnError extends boolean = false>(options: Options<GetGroupInvitationsData, ThrowOnError>) => (options.client ?? client).get<GetGroupInvitationsResponses, GetGroupInvitationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/invitations',
    ...options
});

/**
 * Add or invite a group member
 *
 * Resolves a `user_id` or `email` into one of four outcomes and returns
 * which outcome occurred via the `outcome` field. HTTP status mirrors the
 * outcome: `201` when something was created (`added`, `invited`), `200`
 * when no work was needed (`already_member`, `invitation_pending`).
 *
 * Resolution rules:
 *
 * - `added`: The request targeted a `user_id` and that user already
 * follows the caller. A group membership is created immediately. The
 * "must follow caller" signal is treated as opt-in, so no invite/accept
 * round trip is required.
 * - `invited`: A new pending invitation was created and an email was sent.
 * - `invitation_pending`: An open invitation for this user or email
 * already exists; the existing invitation is returned unchanged.
 * - `already_member`: The user is already a member; no action is taken.
 *
 * When invited by `email` and no user with that email exists, the caller
 * must have permission to invite new users.
 *
 * **Authentication required.**
 *
 */
export const createGroupInvitation = <ThrowOnError extends boolean = false>(options: Options<CreateGroupInvitationData, ThrowOnError>) => (options.client ?? client).post<CreateGroupInvitationResponses, CreateGroupInvitationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke a pending group invitation
 *
 * Revokes a pending invitation so the invitee can no longer accept it.
 * Can be called by the invitation creator or by any user who can manage
 * invitations on the target group.
 *
 * Returns `422` if the invitation is no longer pending (already accepted,
 * declined, or revoked).
 *
 * **Authentication required.**
 *
 */
export const revokeGroupInvitation = <ThrowOnError extends boolean = false>(options: Options<RevokeGroupInvitationData, ThrowOnError>) => (options.client ?? client).delete<RevokeGroupInvitationResponses, RevokeGroupInvitationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/invitations/{invitation_id}',
    ...options
});

/**
 * Delete group invite
 *
 * Deletes the group's shareable invite code. Idempotent: returns `204`
 * whether or not an invite code existed.
 *
 * **Authentication required.**
 *
 */
export const deleteGroupInvite = <ThrowOnError extends boolean = false>(options: Options<DeleteGroupInviteData, ThrowOnError>) => (options.client ?? client).delete<DeleteGroupInviteResponses, DeleteGroupInviteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/invite',
    ...options
});

/**
 * Get group invite
 *
 * Returns the group's shareable invite code and public invite URL. Only
 * users who can manage the group may view the reusable invite code.
 *
 * **Authentication required.**
 *
 */
export const getGroupInvite = <ThrowOnError extends boolean = false>(options: Options<GetGroupInviteData, ThrowOnError>) => (options.client ?? client).get<GetGroupInviteResponses, GetGroupInviteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/invite',
    ...options
});

/**
 * Create or reuse group invite
 *
 * Creates the group's shareable invite code if it does not exist, or
 * returns the existing one. Only users who can manage the group may create
 * or view the reusable invite code.
 *
 * **Authentication required.**
 *
 */
export const createGroupInvite = <ThrowOnError extends boolean = false>(options: Options<CreateGroupInviteData, ThrowOnError>) => (options.client ?? client).post<CreateGroupInviteResponses, CreateGroupInviteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/invite',
    ...options
});

/**
 * Get group contents
 *
 * Returns paginated contents (blocks and channels) created by a group.
 * Uses the search API to find all content added by the specified group.
 * Respects visibility rules and user permissions.
 */
export const getGroupContents = <ThrowOnError extends boolean = false>(options: Options<GetGroupContentsData, ThrowOnError>) => (options.client ?? client).get<GetGroupContentsResponses, GetGroupContentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/contents',
    ...options
});

/**
 * Get group followers
 *
 * Returns paginated list of users who follow this group.
 * All followers are users.
 *
 */
export const getGroupFollowers = <ThrowOnError extends boolean = false>(options: Options<GetGroupFollowersData, ThrowOnError>) => (options.client ?? client).get<GetGroupFollowersResponses, GetGroupFollowersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/v3/groups/{id}/followers',
    ...options
});

/**
 * Search across Are.na
 *
 * Search across blocks, channels, users, and groups.
 *
 * **⚠️ Premium Only**: This endpoint requires a Premium subscription.
 *
 * **Examples:**
 * - Simple: `/v3/search?query=brutalism`
 * - Images only: `/v3/search?query=architecture&type=Image`
 * - My content: `/v3/search?query=*&scope=my`
 * - In a channel: `/v3/search?query=design&channel_id=12345`
 * - By a user: `/v3/search?query=*&user_id=456`
 * - PDFs sorted by date: `/v3/search?query=*&ext=pdf&sort=created_at_desc`
 *
 */
export const search = <ThrowOnError extends boolean = false>(options?: Options<SearchData, ThrowOnError>) => (options?.client ?? client).get<SearchResponses, SearchErrors, ThrowOnError>({
    querySerializer: { parameters: { type: { array: { explode: false } }, ext: { array: { explode: false } } } },
    security: [{ scheme: 'bearer', type: 'http' }, { scheme: 'bearer', type: 'http' }],
    url: '/v3/search',
    ...options
});
